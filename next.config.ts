import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "images.unsplash.com",
      "cdn.pixabay.com",
      "cdn.rareblocks.xyz",
      "images.pexels.com",
      "example.com",
      "pikaso.cdnpk.net",
      "source.unsplash.com",
      "randomuser.me",
    ],
  },
};

export default nextConfig;
